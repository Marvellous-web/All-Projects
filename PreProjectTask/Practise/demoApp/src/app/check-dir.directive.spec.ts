import { CheckDirDirective } from './check-dir.directive';

describe('CheckDirDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckDirDirective();
    expect(directive).toBeTruthy();
  });
});
